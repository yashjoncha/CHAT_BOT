services:
  - type: web
    name: chat-bot
    env: docker
    plan: free
    healthCheckPath: /
    envVars:
      - key: DEBUG
        value: False
      - key: SECRET_KEY
        generateValue: true
      - key: ALLOWED_HOSTS
        value: .onrender.com
